# WearSmart FastAPI Project - Requirements
# Install with: pip install -r requirements.txt

# ===========================================
# Core FastAPI Dependencies
# ===========================================
fastapi>=0.104.1              # FastAPI web framework
uvicorn[standard]>=0.24.0     # ASGI server to run FastAPI
pydantic>=2.0.0               # Data validation (included with FastAPI but explicit)
python-multipart>=0.0.6       # For handling file uploads and form data

# ===========================================
# Machine Learning Dependencies
# ===========================================
joblib>=1.3.2                 # For loading .pkl model files
pandas>=2.0.0                  # Data manipulation for ML models
scikit-learn>=1.3.0           # Required for joblib models (ML algorithms)

# ===========================================
# Database & Cloud Storage
# ===========================================
pymongo>=4.6.0                # MongoDB driver for database operations
cloudinary>=1.36.0            # Cloudinary SDK for image storage
python-dotenv>=1.0.0          # Load environment variables from .env file

# ===========================================
# HTTP & Testing
# ===========================================
requests>=2.31.0              # HTTP library for API testing and external requests
fastapi
uvicorn
pydantic
pandas
joblib
scikit-learn
pymongo
cloudinary
python-multipart
requests

# ===========================================
# Optional Dependencies (for other scripts)
# ===========================================
# Uncomment if you need these features:
# streamlit>=1.28.0           # For Streamlit UI apps
# gradio>=4.0.0               # For Gradio UI apps
# torch>=2.0.0                # PyTorch (for BLIP image captioning)
# transformers>=4.30.0        # Hugging Face transformers (for BLIP)
# Pillow>=10.0.0              # PIL/Pillow for image processing
